= form_for  [@person, @friendship], html: { class: "form-horizontal", role: "form" } do |f|
  -if @friendship.errors.any?
    .alert.alert-danger.alert-dismissable{role: "alert"}
      %button.close{type: "button", data: {dismiss: "alert"} }
        %span{aria: {hidden: "true"} } &times;
        %span.sr-only Close
      %h4= "#{pluralize(@comment.errors.count, "error")} prohibited this comment from being saved:"

      %ul
        - @friendship.errors.full_messages.each do |msg|
          %li= msg
  .form-group
    = f.hidden_field :person_id
    = f.hidden_field :friend_id
    = f.label :search_friends, class: 'col-sm-2 control-label'
    .col-sm-10
      = f.text_field :search_friends, class: 'form-control'
  .form-group
    .col-sm-offset-2.col-sm-10
      = f.submit class: 'btn btn-primary'
